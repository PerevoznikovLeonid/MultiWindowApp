<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="MultiWindowApp.Components.InputComponent"
             x:Name="Component">
  <Grid ColumnDefinitions="*, 2.5*">
    <Label Content="{Binding ElementName=Component, Path=Label, Mode=OneWay}"
           Target="{Binding ElementName=Input}"
           Grid.Column="0" />
    <TextBox x:Name="Input"
             PlaceholderText="{Binding ElementName=Component, Path=PlaceholderText, Mode=OneWay}"
             Text="{Binding ElementName=Component, Path=Value, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
             IsReadOnly="{Binding ElementName=Component, Path=IsReadOnly, Mode=OneWay}"
             PasswordChar="{Binding ElementName=Component, Path=PasswordChar, Mode=OneWay}"
             Grid.Column="1" />
  </Grid>
</UserControl>
